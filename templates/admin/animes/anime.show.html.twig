{% extends 'admin/dashboard.html.twig' %}

{% block content %}
    <div class="action--back">
        <a href="{{ path("admin.anime.index") }}" class="btn btn-primary"><i class="fas fa-chevron-left"></i> Retour</a>
    </div>
    <hr />
    <div class="anime--informations">
        <h1>{{ anime.originalTitle }}<br />
        ({{ anime.translatedTitle }})</h1>
        <img src="{{ asset('uploads/anime_image/' ~ anime.image) }}" />
    </div>
{% endblock %}