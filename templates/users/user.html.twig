{% extends 'front.base.html.twig' %}

{% block content %}
    <div class="user">
        <h1>{{ user.username }}</h1>
        <div class="user__informations">
            <p>{{ user.mail }}</p>
            <p>{{ user.status }}</p>
            <img src="{{ user.avatar }}">
        </div>
    </div>
{% endblock %}